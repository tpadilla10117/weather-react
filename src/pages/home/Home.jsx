import React from 'react';

import { SearchBox } from '../../components/utils';

const HomePage = () => {
    return (
        <main>
            <SearchBox />
        </main>
    );
};

export default HomePage;