import React from 'react';

import { SearchBox } from '../../components/utils';
import './Home.css';

const HomePage = () => {
    return (
        <main>
            <SearchBox />
        </main>
    );
};

export default HomePage;